/**
 * DEPRECATED — Las credenciales de correo están ahora en variables de entorno del backend.
 * Ver backend/.env: SMTP_HOST, SMTP_USER, SMTP_PASSWORD
 * Este archivo se mantiene vacío para no romper posibles imports.
 */
